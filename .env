CODE=IcanDoAnyThing

# Backend endpoints. Override via .env.development or .env.production
# (or shell env) when pointing at staging / local API.
API_URI=https://stc.training
WS_HOST=stc.training

# Google OAuth client id for "Sign in with Google" (Google Identity Services).
# The domains using this id (https://stc.training, http://localhost:9000) must
# be listed as Authorized JavaScript origins in the Google Cloud Console.
SOCIAL_AUTH_GOOGLE_CLIENT_ID=812696144817-l1pdrq5sjmd71f5gnv6scsahsfaa5eve.apps.googleusercontent.com
